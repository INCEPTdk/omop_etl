version: '3.5'

services:
  etl:
    build:
      context: .
      args:
        - "COMMIT_SHA=dev"
        - "GITHUB_TAG=dev"
        - "DEV_BUILD=1"
    volumes:
      - "./log:/log"
      #- "/path/to/data:/data"
    networks:
      - etl-net
    env_file:
      - db.env
    environment:
      VERBOSITY_LEVEL: "DEBUG"

networks:
  etl-net:
    name:
      rigs-net
    external: true
